# NVIDIA NIM (OpenAI-compatible) credentials — server-side only.
# Get a key at https://build.nvidia.com  (starts with "nvapi-")
NIM_API_KEY=nvapi-your-key-here

# Optional overrides:
# Any DeepSeek model available on NVIDIA NIM (note the underscore in the id).
NIM_MODEL=deepseek-ai/deepseek-v3_1
# Base URL for the OpenAI-compatible endpoint.
NIM_BASE_URL=https://integrate.api.nvidia.com/v1
